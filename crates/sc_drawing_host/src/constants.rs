pub const MIN_TEXT_WIDTH: i32 = 40;
pub const MIN_TEXT_HEIGHT: i32 = 20;

pub const TEXT_LINE_HEIGHT_SCALE: f32 = 1.35;

// Hit slop for text box corner handles (in pixels). Make this a bit larger than the generic
// handle detection radius so resizing text boxes feels easier.
pub const TEXT_HANDLE_DETECTION_RADIUS: i32 = 12;
